### All of Us CRAM files Directories

AoU keeps a CRAM manifest file that contains the directories for all CRAM and the corresponding CRAM index files in google cloud address ("gs://"). This manifest file is stored at `gs://fc-aou-datasets-controlled/v8/wgs/cram/manifest.csv`

It contains three columns: `person_id`, `cram_uri`, and `cram_index_uri`.

The URI addresses of CRAM and index files will be needed in following steps. 
